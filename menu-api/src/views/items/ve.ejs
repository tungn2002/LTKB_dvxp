
        <%- include('header') %>
        <!-- Page Content -->
        <div id="page-content-wrapper">
            <nav class="navbar navbar-expand-lg navbar-light bg-transparent py-4 px-4">
                <div class="d-flex align-items-center">
                    <i class="fas fa-align-left primary-text fs-4 me-3" id="menu-toggle"></i>
                    <h2 class="fs-2 m-0">Quản lý vé</h2>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <i class="fas fa-user me-2"></i>Admin
                    </ul>
                </div>
            </nav>

            <div class="container-fluid px-4">
                <div class="row my-5">
                    <div class="col">
                        <table class="table bg-white rounded shadow-sm  table-hover">
                            <thead>
                                <tr>
                                    <th scope="col" width="50">id</th>
                                    <th scope="col">Khách hàng</th>
                                    <th scope="col">Ghế</th>
                                    <th scope="col">Ngày mua</th>
                                    <th scope="col">Tùy chọn</th>

                                </tr>
                            </thead>
                            <tbody>
                                
                                <% for (let p of list)  {%>
                                <tr>
                                    <th scope="row"><%= p.idve %></th>
                                    <td><%= p.idkh %></td>
                                    <td><%= p.idghe %></td>
                                    <td><%= p.ngaymua %></td>
                                    <td>  <form action="/api/menu/items/editve/<%= p.idve %>" method="get"><button class="btn btn-primary">Sửa</button></form>  
                                        <form action="/api/menu/items/deleteve/<%= p.idve %>?_method=delete" method="POST"><button class="btn btn-primary">Xóa</button></form></td>
                                </tr>
                                <% } %>

                            </tbody>
                        </table>
                        <form action="/api/menu/items/addve" method="get">
                                <button type="submit" class="btn btn-primary">Thêm</button>
                        </form>

                    </div>
                </div>

            </div>
        </div>
    </div>
    

    <!-- /#page-content-wrapper -->    
    <%- include('footer') %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"></script>

    <script src="/js/app.js"></script>

</body>
</html>